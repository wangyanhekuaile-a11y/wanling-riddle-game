# 推送到工蜂的完整操作步骤

由于无法直接访问工蜂平台（需要腾讯内部登录认证），请您按照以下步骤手动操作。

## 📋 前提条件

- ✅ 已登录工蜂账号（yanhewang）
- ✅ 已准备好 Personal Access Token（如果需要）
- ✅ 代码已在本地提交完成

## 🚀 推送步骤

### 第 1 步：在工蜂创建项目

1. **访问工蜂**：
   ```
   https://git.code.oa.com/
   ```

2. **创建新项目**：
   - 点击右上角的 "New Project" 或 "新建项目"
   - 填写项目信息：
     - **Project name（项目名称）**：`smart-riddle-game`
     - **Project description（项目描述）**：`智慧猜灯谜互动游戏 - 传统节日互动体验`
     - **Visibility Level（可见性）**：
       - 选择 `Internal`（内部可见）或 `Private`（私有）
     - 点击 "Create project" 或 "创建项目"

3. **复制仓库地址**：
   - 项目创建成功后，会显示仓库地址
   - 地址格式可能是：
     ```
     HTTPS: https://git.code.oa.com/yanhewang/smart-riddle-game.git
     SSH: git@git.code.oa.com:yanhewang/smart-riddle-game.git
     ```
   - 复制 HTTPS 地址（推荐）

### 第 2 步：推送代码到工蜂

在终端执行以下命令：

```bash
# 1. 确保在项目目录中
cd /Users/hehewang/WorkBuddy/20260316115522

# 2. 查看当前 Git 配置（确认已配置用户信息）
git config user.name
git config user.email

# 如果没有配置，执行：
# git config user.name "yanhewang"
# git config user.email "yanhewang@tencent.com"

# 3. 添加远程仓库（替换为您的实际地址）
git remote add origin https://git.code.oa.com/yanhewang/smart-riddle-game.git

# 4. 查看远程仓库配置（确认添加成功）
git remote -v

# 5. 推送代码到远程仓库
git push -u origin main
```

### 第 3 步：处理身份验证

推送时会提示输入用户名和密码：

```
Username for 'https://git.code.oa.com/yanhewang': yanhewang
Password for 'https://yanhewang@git.code.oa.com/yanhewang':
```

**重要**：密码处输入 **Personal Access Token**，而不是工蜂登录密码！

#### 如何获取 Personal Access Token？

1. **访问工蜂个人设置**：
   - 登录工蜂后，点击右上角头像
   - 选择 "Settings" 或 "设置"

2. **生成访问令牌**：
   - 左侧菜单选择 "Access Tokens" 或 "访问令牌"
   - 点击 "Add new token" 或 "添加新令牌"

3. **配置令牌**：
   - **Token name（令牌名称）**：`smart-riddle-game-deploy`（或其他）
   - **Expiration date（过期日期）**：选择合适的时间（如 1 年）
   - **Scopes（权限范围）**：勾选以下权限：
     - ✅ `read_repository`（读取仓库）
     - ✅ `write_repository`（写入仓库）
   - 点击 "Create personal access token" 或 "创建令牌"

4. **复制令牌**：
   - 令牌只显示一次，请立即复制并妥善保存
   - 格式类似：`glpat-xxxxxxxxxxxxxxxxxxxx`

5. **使用令牌**：
   - 在 Git push 的密码提示处粘贴令牌
   - **注意**：输入密码时不会显示任何字符，直接粘贴并回车

### 第 4 步：验证推送成功

推送成功后会看到类似输出：

```
Enumerating objects: 15, done.
Counting objects: 100% (15/15), done.
Delta compression using up to 8 threads
Compressing objects: 100% (11/11), done.
Writing objects: 100% (15/15), 10.50 KiB | 2.62 MiB/s, done.
Total 15 (delta 3), reused 0 (delta 0)
To https://git.code.oa.com/yanhewang/smart-riddle-game.git
 * [new branch]      main -> main
```

然后在工蜂项目页面确认：
- ✅ 查看文件列表（应该有 index.html, README.md 等）
- ✅ 查看 Commit 历史（应该有 4 次提交）
- ✅ 查看代码内容（确认上传成功）

### 第 5 步：配置分支保护（可选但推荐）

1. **进入项目设置**：
   - 在工蜂项目页面
   - 点击 "Settings" → "Repository" → "Protected branches"

2. **添加保护分支**：
   - Branch：选择 `main`
   - 勾选以下保护规则：
     - ✅ `Allowed to push`：选择 `Maintainers`（仅维护者可推送）
     - ✅ `Allowed to merge`：选择 `Maintainers`（仅维护者可合并）
   - 点击 "Protect"

## 🔧 常见问题

### Q1: 提示 "Permission denied" 或 "Authentication failed"

**解决方案**：
1. 确认用户名正确（你的工蜂账号）
2. 确认使用 Personal Access Token，不是登录密码
3. 检查令牌权限是否包含 `write_repository`
4. 如果令牌过期，重新生成新的令牌

### Q2: 提示 "remote origin already exists"

**解决方案**：
```bash
# 查看当前远程仓库
git remote -v

# 如果地址不对，先删除再添加
git remote remove origin

# 重新添加正确的地址
git remote add origin https://git.code.oa.com/yanhewang/smart-riddle-game.git
```

### Q3: 提示 "error: failed to push some refs"

**解决方案**：
```bash
# 强制推送（谨慎使用，会覆盖远程）
git push -u origin main --force

# 或先拉取再推送（推荐）
git pull origin main --allow-unrelated-histories
git push -u origin main
```

### Q4: 想要使用 SSH 而不是 HTTPS

**解决方案**：
```bash
# 添加 SSH 远程地址
git remote set-url origin git@git.code.oa.com:yanhewang/smart-riddle-game.git

# 确保已配置 SSH 密钥
# 如果没有，需要生成并添加到工蜂
ssh-keygen -t rsa -b 4096 -C "yanhewang@tencent.com"
# 然后将 ~/.ssh/id_rsa.pub 的内容添加到工蜂 SSH Keys
```

### Q5: 如何查看推送到工蜂的代码？

**方法**：
1. 访问工蜂项目页面
2. 点击 "Repository" → "Files"
3. 可以查看所有文件和代码内容
4. 点击 "Commits" 查看提交历史

## 📝 推送后的项目地址

成功推送后，您的工蜂项目地址将是：

```
https://git.code.oa.com/yanhewang/smart-riddle-game
```

可以直接访问这个地址查看项目详情。

## ✅ 推送成功后下一步

推送成功后，您可以：

1. **配置 CI/CD**（可选）：
   - 按照 `DEPLOYMENT_GUIDE.md` 中的工蜂 CI/CD 部分配置
   - 实现代码推送自动部署

2. **开始部署上线**：
   - 参考 `DEPLOYMENT_GUIDE.md` 选择部署方案
   - 推荐使用 COS + CDN 方案

3. **获取游戏链接**：
   - 完成部署后，获得正式的游戏链接
   - 例如：`https://game.yourdomain.com/index.html`

## 🎯 快速命令汇总

```bash
# 完整的推送命令（一键执行）
cd /Users/hehewang/WorkBuddy/20260316115522 && \
git remote add origin https://git.code.oa.com/yanhewang/smart-riddle-game.git && \
git push -u origin main

# 查看远程仓库状态
git remote -v

# 查看提交历史
git log --oneline

# 查看当前状态
git status
```

## 📞 需要帮助？

如遇到问题，请参考：

- **DEPLOYMENT_GUIDE.md**：详细的部署指南
- **QUICKSTART.md**：快速开始指南
- **工蜂文档**：https://git.code.oa.com/help
- **Git 官方文档**：https://git-scm.com/doc

---

**祝您推送顺利！** 🚀

完成后请继续查看 `DEPLOYMENT_GUIDE.md` 进行部署上线。
